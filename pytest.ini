[pytest]
# No pythonpath. It used to say `vasco`, which is what made `from models.qstarmodel import ...`
# resolve from inside the package -- the same bare-import layout that kept vasco from being
# installable at all. The package is importable now, so the tests import it by name.
